name: My workflow

on: push

jobs:
  my-job:
    name: My job
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to http://equal-education.surge.sh/
        uses: dswistowski/surge-sh-action@v1
        with:
          domain: 'http://equal-education.surge.sh/'
          project: '.'
          token: ${{ secrets.  c03a519c0a90eac21b63a94ff6bf8657}}